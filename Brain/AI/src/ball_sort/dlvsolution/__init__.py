from AI.src.ball_sort.dlvsolution.helpers import Color

'''
def get_ancestor_classes(cls):
    ancestors = []
    for ancestor in cls.__bases__:
        ancestors.append(ancestor)
        ancestors.extend(get_ancestor_classes(ancestor))
    return ancestors

print(f"COLOR has:{get_ancestor_classes(Color)}")
'''